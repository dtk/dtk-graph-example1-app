import app from './app';
import db from './db';
import middlewares from './middlewares';
import routes from './routes';
import start from './start';

export default {
  db,
  middlewares,
  routes,
  app,
  start
};
